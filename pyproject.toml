[tool.ruff]
line-length = 88
target-version = "py310"
select = ["F", "E", "I", "B"]
ignore = ["E203"]  # ← W503 を削除！
exclude = ["venv", ".venv", "__pycache__"]

[tool.mypy]
python_version = "3.10"
strict = true
exclude = ["venv", ".venv"]
