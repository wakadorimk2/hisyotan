[tool.ruff]
line-length = 88
target-version = "py310"
lint.select = ["F", "E", "I", "B"]
lint.ignore = ["E203", "E402", "E501"]  # ← W503 を削除！
exclude = ["venv", ".venv", "__pycache__"]

[tool.mypy]
python_version = "3.10"
strict = true
exclude = ["venv", ".venv"]
